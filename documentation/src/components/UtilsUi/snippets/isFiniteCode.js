const isFiniteCode = `import { isFinite } from '@cristatus/utils';

const isFiniteNumber = isFinite(10); // true

const isInfiniteNumber = isFinite(Infinity); // false
`;

export default isFiniteCode;
