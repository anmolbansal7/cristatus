const startCaseCode = `import { startCase } from '@cristatus/utils';

// Convert a string to start case.
const startCaseString = startCase("hello_world"); //Hello World
`;

export default startCaseCode;
