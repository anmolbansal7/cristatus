const prefersDarkMode = `import { prefersDarkMode } from '@cristatus/utils';

const isDarkMode = prefersDarkMode(); // True if Dark Mode is enabled in system

console.log('isDarkMode:: ', isDarkMode);`;

export default prefersDarkMode;
